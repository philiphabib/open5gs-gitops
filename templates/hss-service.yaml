apiVersion: v1
kind: Service
metadata:
  name: ims-hss
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: ims-hss
  ports:
    - name: diameter
      port: 3868
      targetPort: 3868
      protocol: TCP
